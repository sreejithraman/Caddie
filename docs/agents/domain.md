# Domain docs

Caddie is a single-context repository.

Before exploring or changing the system, read `CONTEXT.md` and the relevant decisions under `docs/adr/`.

Use the glossary's canonical terms in issues, tests, specifications, and implementation. Surface conflicts with an existing ADR before proceeding.
